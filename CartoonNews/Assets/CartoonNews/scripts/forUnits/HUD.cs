using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HUD : MonoBehaviour {
public GameObject HelthBar;


	// Use this for initialization
	void Awake() {
		
		//HelthBar[1]=GameObject.FindGameObjectWithTag(Lib.UseTag(Lib.UTags.Player));
		//HelthBar[0]=GetComponent<>();


		//PlayerH=GameObject.FindGameObjectWithTag(Lib.UseTag(Lib.UTags.Player)).GetComponent<DriverAnimCharacter>();
	}
	
	// Update is called once per frame
	void Update ()
	{
		
	}
}